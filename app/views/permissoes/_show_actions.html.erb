<%- model_class = Permissao -%>

<% remote ||= remote %>

<%= link_to edit_permissao_path(permissao), :class => 'btn btn-warning', :remote => remote do  %>
	<%= t("helpers.links.edit") %>
<% end %>

<%= link_to permissao_path(permissao),
    :remote => remote,
	:method => 'delete',
	:data => { :confirm => t("helpers.links.confirm_destroy",
	:model => model_class.model_name.human) },
	:class => 'btn btn-danger' do %>
	<%= t("helpers.links.destroy") %>
<% end %>